SQL_RULES = [
    "Always include LIMIT clause if user asks to limit rows.",
    "Prefer selecting only needed columns unless the user says 'all'.",
    "Only generate the SQL query. Do not explain it."
]
